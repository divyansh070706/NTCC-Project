const generateInsights = () => {
  return [
    "Revenue increased significantly this month.",
    "Customer engagement is improving steadily.",
    "Electronics category shows highest growth.",
  ];
};

export default generateInsights;